// Quando importamos mais de uma função precisamos declara-las entre chaves.
// Passo 3 - Importamos a função.
import {soma,multiplica,divide} from "./modulo.mjs"

console.log(soma(50, 25));
console.log(multiplica(50,25));
// Passo 4 - Chamamos a função
console.log(divide(16, 2));
