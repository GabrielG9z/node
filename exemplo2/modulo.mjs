function soma(valor1, valor2){
    return valor1 + valor2;
};

function multiplica(valor1, valor2){
    return valor1 * valor2;
};

// Quando exportamos mais de uma 'função', precisamos declarar entre chaves.
export default {soma,multiplica};